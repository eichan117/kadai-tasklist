<% if user.any? %>

<ul>
    <%= users.each do |user| %>
  <li>
    <img src="<%= gravatar_url(user,{size50}) %>" alt="">
<%= user.name %>

<p><%= link_to 'view profile',user_path(user) %></p>
   
   </li>

<% end %>
   
</ul>

<%= paginate users %>

<% end %>