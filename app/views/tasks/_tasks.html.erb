<h1>タスク一覧</h1>
<ul>

<% @tasks.each do |tasks| %>
 
 <li><%= link_to tasks.id,tasks %>
 <span>posted at <%= tasks.created_at %></span>
  <p><%= tasks.content %></p>
 </li>

<% end %>  

</ul>

<%= link_to 'Logout',login_path %>

<%= link_to 'タスクを投稿する',new_task_path %>
